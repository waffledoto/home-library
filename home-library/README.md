# ☕ Домашняя Библиотека

Веб-приложение для управления личной коллекцией книг с встроенным PDF-ридером. Разработано с использованием Next.js, TypeScript, PostgreSQL и Tailwind CSS.

## 🎨 Дизайн

Приложение выполнено в красивых кофейных тонах с градиентами и современным UI:
- **3 колонки** для управления статусом книг: "Не начатые", "Читаю", "Прочитанные"
- **Встроенный PDF-ридер** для чтения книг прямо на сайте
- **Адаптивный дизайн** для работы на любых устройствах
- **Красивые анимации** и плавные переходы

## ✨ Функциональность

### Управление книгами
- ➕ Добавление новых книг с обложкой, описанием и файлом книги
- ✏️ Изменение статуса книги (не начата / читаю / прочитана)
- 📊 Отслеживание прогресса чтения по страницам
- 💾 Сохранение прогресса чтения
- 🗑️ Удаление книг

### PDF-ридер
- 📖 Чтение PDF файлов прямо в браузере
- 📁 Возможность открыть локальный PDF-файл с компьютера
- 🔄 Автоматическое сохранение текущей страницы
- 📱 Удобный интерфейс для навигации по страницам

### Загрузка файлов
- 🖼️ Загрузка обложек книг (JPEG, PNG, WebP, GIF)
- 📚 Загрузка файлов книг (PDF, EPUB, FB2, MOBI, DJVU, TXT)
- 💾 Файлы хранятся в директории `public/uploads`

## 🚀 Быстрый старт

### Шаг 1: Настройка базы данных (Supabase PostgreSQL)

> **Важно:** Приложение использует облачную базу данных Supabase (бесплатно, работает в РФ).

1. **Зарегистрируйтесь на [Supabase](https://supabase.com)**
2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите бесплатный тариф
   - Дождитесь создания базы (~2 минуты)
3. **Получите строку подключения:**
   - Перейдите в **Settings** (шестерёнка) → **Database**
   - Найдите раздел **Connection string**
   - Выберите режим **Transaction** (порт 6543)
   - Скопируйте строку (выглядит как `postgresql://postgres.xxx:...`)
4. **Добавьте в переменные окружения:**
   - Скопируйте файл `.env.local.example` в `.env.local` (если ещё не создан)
   - Вставьте строку подключения в `POSTGRES_URL`

```env
POSTGRES_URL=postgresql://postgres.xxxxxxxxxxxxxxxx:YOUR_PASSWORD@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
```

> **Примечание:** Таблица создастся автоматически при первом запуске!

### Шаг 2: Установка и запуск

#### Требования
- Node.js 18+
- npm или yarn

#### Установка зависимостей
```bash
npm install
```

#### Запуск в режиме разработки
```bash
npm run dev
```

Приложение будет доступно по адресу: [http://localhost:3000](http://localhost:3000)

#### Сборка для продакшена
```bash
npm run build
npm start
```

## 🌐 Деплой на Vercel

1. **Запушьте код на GitHub**
```bash
git add .
git commit -m "Initial commit"
git push
```

2. **Подключите проект на [Vercel](https://vercel.com)**
   - Войдите через GitHub
   - Нажмите "New Project"
   - Импортируйте ваш репозиторий

3. **Добавьте переменную окружения:**
   - Перейдите в **Settings** → **Environment Variables**
   - Добавьте `POSTGRES_URL` со строкой подключения из Supabase
   - Нажмите "Save"

4. **Задеплойте!**
   - Vercel автоматически соберёт и запустит приложение

## 📁 Структура проекта

```
home-library/
├── app/
│   ├── api/                    # API маршруты (бэкенд)
│   │   ├── books/              # CRUD операции с книгами
│   │   ├── upload/             # Загрузка файлов
│   │   └── files/books/        # Скачивание файлов книг
│   ├── page.tsx                # Главная страница
│   ├── layout.tsx              # Основной layout
│   └── globals.css             # Глобальные стили
├── components/
│   ├── Header.tsx              # Шапка приложения
│   ├── BookColumn.tsx          # Колонка для книг
│   ├── BookCard.tsx            # Карточка книги
│   ├── AddBookModal.tsx        # Модальное окно добавления
│   └── PDFReader.tsx           # PDF-ридер
├── lib/
│   └── db.ts                   # Подключение к PostgreSQL
├── public/
│   └── uploads/                # Загруженные файлы (covers, books)
└── .env.local                  # Переменные окружения (не коммитить!)
```

## 🛠️ Технологии

- **Frontend**: Next.js 16, React, TypeScript, Tailwind CSS
- **Backend**: Next.js API Routes
- **База данных**: PostgreSQL (Supabase)
- **Драйвер БД**: postgres.js (postgres)
- **Стилизация**: Tailwind CSS с кастомной палитрой кофейных цветов

## 🎨 Цветовая палитра

Приложение использует красивую кофейную палитру:

| Цвет | Код | Использование |
|------|-----|---------------|
| ☕ Светло-кофейный | `#fdf8f6` | Фон |
| 🤎 Кофейный | `#a0937d` | Кнопки, акценты |
| 🍫 Тёмный кофе | `#6f5e4f` | Заголовки, шапка |
| 🌰 Эспрессо | `#4a3b32` | Текст |

## 📖 Использование

### Добавление книги

1. Нажмите кнопку **"Добавить книгу"** в шапке
2. Заполните поля:
   - **Название книги** (обязательно)
   - **Автор** (обязательно)
   - **Описание** (необязательно)
   - **Обложка** (необязательно, изображение)
   - **Файл книги** (необязательно, PDF)
3. Нажмите **"Добавить книгу"**

### Чтение книги

1. Кликните на карточку книги
2. Откроется PDF-ридер
3. Если у книги есть PDF-файл, он загрузится автоматически
4. Можно открыть локальный PDF-файл кнопкой **"Открыть PDF"**

### Изменение статуса

1. В PDF-ридере нажмите на кнопку статуса
2. Выберите новый статус:
   - 📚 **Не начата** - книга ещё не начата
   - 📖 **Читаю** - книга читается
   - ✅ **Прочитана** - книга прочитана
3. Нажмите **"Сохранить"** для сохранения прогресса

## 🔌 API Эндпоинты

### Книги
- `GET /api/books` — Получить все книги
- `POST /api/books` — Создать новую книгу
- `PUT /api/books/[id]` — Обновить книгу
- `DELETE /api/books/[id]` — Удалить книгу

### Загрузка файлов
- `POST /api/upload` — Загрузить файл (обложка или книга)
- `DELETE /api/upload?path=/uploads/...` — Удалить файл
- `GET /api/files/books/[filename]` — Скачать/просмотреть файл книги

## 📝 Примечания

- База данных PostgreSQL размещена в Supabase (облако)
- Загруженные файлы сохраняются в `public/uploads/`
- Приложение поддерживает как загруженные на сервер файлы, так и локальные файлы
- Для продакшена рекомендуется использовать Supabase Storage или Vercel Blob для хранения файлов

## 🐛 Решение проблем

### Ошибка подключения к базе данных
```
Database URL not found. Set POSTGRES_URL...
```
**Решение:** Убедитесь, что файл `.env.local` существует и содержит правильную строку подключения из Supabase.

### Ошибка SSL подключения
**Решение:** Убедитесь, что используете строку подключения в режиме **Transaction** (порт 6543), а не Session (порт 5432).

### Ошибка при запуске
Убедитесь, что все зависимости установлены:
```bash
npm install
```

## 📄 Лицензия

MIT

## 👨‍💻 Автор

Разработано для курсовой работы

---

**Наслаждайтесь чтением! ☕📚**
